{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p \"$(dirname \"${DB_DATABASE:-database/database.sqlite}\")\" && touch \"${DB_DATABASE:-database/database.sqlite}\" && php artisan storage:link --force && php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
  }
}